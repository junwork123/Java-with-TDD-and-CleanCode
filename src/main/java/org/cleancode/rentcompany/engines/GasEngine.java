package org.cleancode.rentcompany.engines;

import org.cleancode.rentcompany.engines.config.EngineConfig;

public class GasEngine extends Engine {
    protected GasEngine(EngineConfig config) {
        super(config);
    }
}
